# DishLens Qwen 线上恢复交接 - 2026-08-11

## 结论

线上新菜单识别失败的根因不是项目所使用的阿里云账户整体欠费，而是生产服务器仍在使用另一把旧 Qwen Key。旧 Key 调用 DashScope 返回 `Arrearage`，同时 Gemini 返回 429，导致两个视觉模型同时失败。

现已将线上识别主模型恢复为 Qwen，冷启动菜单可以正常返回结果。随后完成了 Qwen3 视觉模型 A/B、生产热切换和失败图片缓存修复；UI 未修改。

## 配置证据

- 本地可用 Qwen Key 指纹：`sha256:75767a682cc0`，长度 35。
- 线上旧 Qwen Key 指纹：`sha256:96888e08bba2`，长度 35。
- 线上新 Qwen Key 指纹：`sha256:75767a682cc0`，长度 35。
- 线上模型顺序已由 `gemini,qwen` 改为 `qwen,gemini`。
- Qwen Base URL 未显式覆盖，继续使用代码默认值 `https://dashscope.aliyuncs.com/compatible-mode/v1`。
- 快速识图主模型已切换为 `qwen3-vl-flash`。
- 质量兜底模型为 `qwen3-vl-plus`。
- 快速首轮页并发为 4，单模型尝试超时为 20s。

生产环境备份：

```text
/opt/dishlens-global/.env.production.bak-2026-08-11T07-10-23-935Z
```

PM2 状态：`dishlens` online，配置替换后已重启。

Qwen3 热切换备份：生产 `.env.production.bak-<UTC>-qwen3-vl-flash`。

## API 探测

使用新 Key 的最小请求：

| 探测 | 状态 | 耗时 | 结果 |
| --- | --- | --- | --- |
| 本地 `qwen-vl-plus` 文本请求 | HTTP 200 | 370ms | `OK` |
| 本地真实菜单视觉请求 | HTTP 200 | 800ms | 识别为英文菜单，约 20 个可见条目 |
| 生产主机文本请求 | HTTP 200 | 1.47s | `OK` |

## 真实菜单回归

### 意大利菜单，单页冷启动

- 菜品数量：42
- 上传接口响应：5.06s
- 首个可用结果：81.11s
- 文本完成：81.11s
- 首次图片可用：40/42
- 结果状态：`done`

该样本出现一次快速模型链重试，单页模型耗时约 74s，说明 Qwen 可用但尾延迟仍然很高。

### 法国菜单，四页 49 道菜冷启动

- 任务 ID：`2356b6df-2bf1-47c0-9ae6-44abe195e671`
- 菜品数量：49
- 页面数量：4
- 上传接口响应：5.37s
- 首个可用结果：24.65s
- 四页文本完成：51.35s
- 各页 Qwen 首轮模型耗时：25.49s、15.82s、19.82s、15.98s
- 初始图片：33/49
- 后台补全后图片：49/49
- 图片 URL：48 个唯一 URL，1 次重复分配
- 图片来源：33 个站内 URL、15 个 Supabase Storage URL
- 图片有效性：48/48 唯一 URL 均返回 HTTP 200
- 结果状态：`done`

### Qwen3 同图 A/B 与生产复测

本地隔离服务、同一四页法国菜单、缓存扰动：

| 模型/并发 | 首个结果 | 四页文本完成 | 菜品 |
| --- | ---: | ---: | ---: |
| `qwen3-vl-flash` / 3 页 | 9.30s | 30.39s | 49 |
| `qwen3-vl-plus` / 3 页 | 13.79s | 39.38s | 49 |
| `qwen3-vl-flash` / 4 页 | 9.25s | 16.79s | 49 |

生产热切换后的冷启动任务 `db68c409-7e92-42dd-aa68-39ee2270aefc`：

- 上传响应：9.76s；其中服务端 intake 0.85s，主要延迟发生在上海到美国中部的跨洲上传。
- 首个结果：26.56s；服务端第一页完成 16.46s。
- 一页 Flash + Plus 均超时后降级到 Gemini，该页耗时 55.28s，证明串行兜底仍会放大尾延迟。
- 初始快照 51 道，后台富化去重后稳定为 49 道。
- 后台完成后 49/49 有图，49 个 URL 均唯一。

原始菜单的旧缓存仍有 15 张失败图。根因有三层：`resultNeedsImageRefresh` 排除了 `image_status=failed`；外层先把状态改成 `processing` 后才重新判断；刷新函数内部又重复做了一次同样的动态判断。现已生成不可变的 `CachedResultRefreshPlan`，从判断、展示状态到后台执行复用同一份计划；缓存失败且仍有缺图时重新入队，只处理缺图，不覆盖成功图片。

## 已解决

1. 线上旧 Qwen Key 导致的 `Arrearage` 错误。
2. Gemini 限流时没有可用视觉兜底的问题。
3. 线上供应商顺序未优先使用 Qwen 的问题。
4. 新菜单冷启动返回 0 道菜的问题。
5. Qwen3 Flash 首轮模型选型和四页并发参数。
6. 失败图片缓存永不重试的问题，包括整体 `failed` 和部分成功 `partial` 两种终态。

## 2026-08-11 图片重试补充修复

- 生产复核发现：首轮 15 张缺图中 5 张生成成功、10 张超时后，缓存状态变成 `partial`；旧判断只对整体 `failed` 开放重试，因此后续重复上传不会再补剩余图片。
- `resultNeedsImageRefresh` 现同时识别 `failed` 与 `partial`，仍只选择没有 URL 的菜品，已有图片不会被替换。
- 万相异步任务轮询由固定 60 秒改为 `MENU_IMAGE_GENERATION_POLL_TIMEOUT_MS`，默认 120 秒，符合官方生产建议的两分钟最终超时。
- 生产图片队列使用单并发、提交间隔 1500ms，避免恢复一份旧菜单时同时提交大量任务触发 429。
- Supabase 任务写入在上传关键路径的等待上限降为 200ms；实测服务端 intake 从约 849ms 降至 232ms。数据库不可用时继续使用内存任务状态，但正式根治仍是迁移到新加坡 Cloud SQL、Redis 和 Cloud Tasks。
- 线上任务 `36981910-bf63-4755-b370-94b3cf8f61cd` 验证：缓存菜单 4.49s 进入结果页，49 道菜完整返回；39 张已有图保持不变，剩余 10 张以单并发后台补齐，最终 49/49 有图、0 失败，日志没有该任务的新 429 或超时。
- 完整 URL 检查发现 `LA PIZZA CIOCCOLATO` 曾被历史覆盖规则错误映射到玛格丽特图片。已删除该错误覆盖，并增加缓存本地图与当前知识匹配结果的一致性校验；错图会被清空后进入 AI 补图，不影响正确图片。
- 错图修复后的生产任务 `2d14df3d-f32e-4ac8-a157-1102603c2809`：4.20s 进入结果页，先返回 48/49 图片并只排队 1 张错图；后台完成后为 49 道菜、49 个唯一图片 URL、49/49 HTTP 200、0 失败。巧克力披萨最终使用独立持久化 URL，不再与玛格丽特披萨共图。

## 遗留事项

1. 本地 Flash 基准已达到 9.25s 首结果，但生产仍为 26.56s；美国中部部署无法对亚洲用户承诺 5-10s。
2. 生产上传 9.76s，其中服务端仅 0.85s；需要迁移到新加坡并采用就近直传对象存储。
3. Qwen 单页尾延迟和串行模型兜底仍会放大到 55s；失败页应转后台重试，并增加供应商熔断。
4. 图片补全虽然最终达到 49/49，但仍依赖站内文件和 Supabase Storage 混合来源，应迁移到统一 GCS + CDN 对象键。
5. 49 道菜存在一次图片重复分配，需要继续收紧菜名归一和图片唯一性规则。
6. Qwen Key 目前仍存在于服务器环境文件中；Google Cloud Singapore 架构落地时迁入 Secret Manager。

## 下一步执行顺序

1. 重传原始法国菜单，验证 `partial` 缓存继续补齐剩余 10 张缺图；如果旧万相模型仍超时，切换新加坡 `wan2.6-t2i` 或 `z-image-turbo` 并继续使用自有对象存储持久化。
2. 将上传接口目标压到 1s 内，只负责存图、哈希、建任务和入队；Supabase task write 不再进入请求关键路径。
3. 失败页不在首轮串行等待 Plus + Gemini，先返回成功页面，失败页进入后台重试队列。
4. 完成 Cloud SQL、Redis、Cloud Tasks、GCS + CDN 的新加坡迁移，并使用新加坡 Model Studio Workspace 对应端点和 Key。
5. 重跑法国、意大利、印度、越南、美国五组海外菜单与弱网浏览器测试。

## 验证记录

- 定向回归：缓存失败重试与万相轮询超时配置 2/2 通过；前序定向回归 3/3 通过。
- ESLint：通过。
- Next.js 生产构建：通过。
- 图片发布诊断：`local_image_assets_missing=0`、`untracked=0`、`deploy_ready=true`。
- 完整逻辑回归：178/179；唯一失败为与本次改动无关的远程图片联系表生成脚本超时，需单独给该测试增加离线 fixture 和短超时。
